% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local.R
\name{hf_download_model}
\alias{hf_download_model}
\title{Download a Model Snapshot for Local Inference}
\usage{
hf_download_model(
  model,
  revision = "main",
  cache_dir = NULL,
  token = NULL,
  local_files_only = FALSE
)
}
\arguments{
\item{model}{Character string. Model ID on the Hugging Face Hub.}

\item{revision}{Character string. Branch, tag, or commit to download.
Use a commit hash for reproducibility.}

\item{cache_dir}{Character string or \code{NULL}. Official Hugging Face cache
directory. \code{NULL} uses the Hub library's configured cache.}

\item{token}{Character string or \code{NULL}. Optional authentication token.
\code{NULL} uses \code{HF_TOKEN}, then legacy \code{HUGGING_FACE_HUB_TOKEN}, via
the package's token helper.}

\item{local_files_only}{Logical. Use only already cached model files.
An incomplete offline cache fails rather than going online.}
}
\value{
A normalized, existing snapshot-directory character path.
}
\description{
Use the official \code{huggingface_hub.snapshot_download()} cache to download a
model's configuration, tokenizer, and safetensors weights. Repository
subdirectories and immutable snapshot paths are preserved. Online calls
can download files; \code{local_files_only = TRUE} never fetches missing Hub files.
}
\details{
Only standard models with safetensors weights are supported, not pickle
weights, adapters, or custom Python code. The download patterns are
\verb{*.json}, \verb{*.safetensors}, \verb{*.txt}, \verb{*.model}, and \verb{*.tiktoken}, including
matching files in subdirectories. Embedding module stacks, when present,
must use a root Transformer followed by built-in Pooling or Normalize
modules. Missing required files or referenced weight shards cause an error.

Python dependencies must already be available for fully offline operation;
\code{local_files_only} controls model files, not reticulate's environment setup.
}
\examples{
\dontrun{
path <- hf_download_model(hf_default_model("embed"))
model <- hf_load_local_model(path, task = "embed")

# Reuse the official cache without downloading model files
path <- hf_download_model(hf_default_model("embed"), local_files_only = TRUE)
}
}
\seealso{
\code{\link[=hf_local_setup]{hf_local_setup()}}, \code{\link[=hf_load_local_model]{hf_load_local_model()}}
}
