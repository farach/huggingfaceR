% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate.R
\name{hf_fill_mask}
\alias{hf_fill_mask}
\title{Fill Mask}
\usage{
hf_fill_mask(
  text,
  model = "google-bert/bert-base-uncased",
  mask_token = "[MASK]",
  top_k = 5,
  token = NULL,
  ...
)
}
\arguments{
\item{text}{Character vector of text(s) containing [MASK] token.}

\item{model}{Character string. Model ID from Hugging Face Hub.
Default: "google-bert/bert-base-uncased".}

\item{mask_token}{Character string. The mask token to use. Default: "[MASK]".
Some models use different tokens like "<mask>".}

\item{top_k}{Integer. Number of top predictions to return. Default: 5.}

\item{token}{Character string or NULL. API token for authentication.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A tibble with columns: text, token, score, filled (the complete text)
}
\description{
Fill in a [MASK] token in text with predicted words.
Commonly used with BERT-style models.
}
\examples{
\dontrun{
# Fill in the blank
hf_fill_mask("The capital of France is [MASK].")

# Get top predictions
hf_fill_mask("Paris is the [MASK] of France.", top_k = 3)

# Use with different mask token
hf_fill_mask("The capital of France is <mask>.", mask_token = "<mask>")
}
}
