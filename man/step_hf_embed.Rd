% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidymodels.R
\name{step_hf_embed}
\alias{step_hf_embed}
\alias{tidy.step_hf_embed}
\alias{tunable.step_hf_embed}
\title{Embedding Recipe Step}
\usage{
step_hf_embed(
  recipe,
  ...,
  role = "predictor",
  trained = FALSE,
  model = "BAAI/bge-small-en-v1.5",
  token = NULL,
  embeddings = NULL,
  skip = FALSE,
  id = recipes::rand_id("hf_embed")
)

\method{tidy}{step_hf_embed}(x, ...)

\method{tunable}{step_hf_embed}(x, ...)
}
\arguments{
\item{recipe}{A recipe object.}

\item{...}{One or more text column selectors (see recipes::selections()).}

\item{role}{Character string. Role for the new embedding variables. Default: "predictor".}

\item{trained}{Logical. Internal use only.}

\item{model}{Character string. Hugging Face model ID for embeddings.
Default: "BAAI/bge-small-en-v1.5".}

\item{token}{Character string or NULL. API token for authentication.}

\item{embeddings}{List. Internal use only (stores embeddings during training).}

\item{skip}{Logical. Should step be skipped when baking? Default: FALSE.}

\item{id}{Character string. Unique ID for this step.}

\item{x}{A step_hf_embed object}
}
\value{
An updated recipe object.
}
\description{
Create text embeddings using a Hugging Face model as part of a tidymodels recipe.
This step converts text columns into embedding features for downstream modeling.
}
\examples{
\dontrun{
library(tidymodels)
library(dplyr)

# Create a recipe with embeddings
rec <- recipe(sentiment ~ text, data = train_data) |>
  step_hf_embed(text, model = "BAAI/bge-small-en-v1.5")

# Use in a workflow
wf <- workflow() |>
  add_recipe(rec) |>
  add_model(logistic_reg()) |>
  fit(data = train_data)
}
}
