% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local.R
\name{hf_load_local_model}
\alias{hf_load_local_model}
\alias{print.hf_local_model}
\title{Load a Reusable Local Model}
\usage{
hf_load_local_model(
  model = NULL,
  task = c("embed", "classify"),
  revision = "main",
  cache_dir = NULL,
  token = NULL,
  local_files_only = FALSE,
  device = "cpu"
)

\method{print}{hf_local_model}(x, ...)
}
\arguments{
\item{model}{Character string or \code{NULL}. A Hub model ID or an existing
local model directory. \code{NULL} resolves \code{\link[=hf_default_model]{hf_default_model()}} for \code{task}.}

\item{task}{Character string. Either \code{"embed"} or \code{"classify"}.}

\item{revision}{Character string. Branch, tag, or commit to download.
Use a commit hash for reproducibility.}

\item{cache_dir}{Character string or \code{NULL}. Official Hugging Face cache
directory. \code{NULL} uses the Hub library's configured cache.}

\item{token}{Character string or \code{NULL}. Optional authentication token.
\code{NULL} uses \code{HF_TOKEN}, then legacy \code{HUGGING_FACE_HUB_TOKEN}, via
the package's token helper.}

\item{local_files_only}{Logical. Use only already cached model files.
An incomplete offline cache fails rather than going online.}

\item{device}{Character string. \code{"cpu"} by default. Other devices, such as
\code{"cuda:0"} or \code{"mps"}, are passed to the user's compatible Python stack.
This function does not install CUDA or select a GPU automatically.}

\item{x}{An \code{hf_local_model} handle.}

\item{...}{Additional arguments (currently unused).}
}
\value{
An \code{hf_local_model} handle containing \code{task}, \code{model}, \code{source}
(\code{"hub"} or \code{"local"}), \code{path}, resolved \code{revision} when known
(otherwise \code{NULL}), \code{requested_revision}, \code{device}, and \code{backend}.
Authentication tokens are not stored in the handle.
}
\description{
Load an opt-in local embedding or text-classification model. The default
device is CPU. A Hub ID is first resolved by \code{\link[=hf_download_model]{hf_download_model()}}; an
existing local directory skips downloading. All backend components load
from that exact directory with \code{local_files_only = TRUE} and
\code{trust_remote_code = FALSE}. There is no hosted inference fallback.
}
\details{
Standard safetensors models are required. Embedding models must include
\code{modules.json} describing a root Transformer followed by built-in Pooling or
Normalize modules. Missing module metadata is rejected rather than falling
back to a different pooling configuration. Arbitrary module loaders, adapters,
and custom code are unsupported.

Python handles are session-specific. Do not use \code{saveRDS()} to transfer a
loaded handle between R sessions. Save the snapshot path instead and call
\code{hf_load_local_model()} again in the new session.
}
\examples{
\dontrun{
model <- hf_load_local_model(task = "embed")
hf_embed_local(c("Hello world", "Goodbye world"), model)

classifier <- hf_load_local_model(task = "classify")
hf_classify_local("I enjoy programming in R.", classifier)

# Reload an already downloaded snapshot in a new session
snapshot_path <- model$path
model <- hf_load_local_model(snapshot_path, task = "embed")
}
}
\seealso{
\code{\link[=hf_local_setup]{hf_local_setup()}}, \code{\link[=hf_embed_local]{hf_embed_local()}}, \code{\link[=hf_classify_local]{hf_classify_local()}}
}
