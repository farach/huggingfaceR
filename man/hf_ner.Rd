% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text-tasks.R
\name{hf_ner}
\alias{hf_ner}
\title{Named Entity Recognition (Token Classification)}
\usage{
hf_ner(
  text,
  model = hf_default_model("ner"),
  aggregation_strategy = "simple",
  token = NULL,
  endpoint_url = NULL,
  ...
)
}
\arguments{
\item{text}{Character vector of text(s) to analyze.}

\item{model}{Character string. Model ID from the Hugging Face Hub. Append
\code{":provider"} to select an inference provider. Default: "dslim/bert-base-NER".}

\item{aggregation_strategy}{Character string. How sub-word tokens are grouped
into entities: one of "none", "simple", "first", "average", "max".
Default: "simple".}

\item{token}{Character string or NULL. API token for authentication.}

\item{endpoint_url}{Character string or NULL. A custom Inference Endpoint URL.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A tibble with columns: text, word, entity_group, score, start, end.
}
\description{
Extract named entities (people, organizations, locations, etc.) from text
using a token-classification model via the Hugging Face Inference Providers
API. Returns one row per detected entity, with character offsets that let you
highlight or join back to the source text. Inputs that produce no entities
(and \code{NA} inputs) yield a single row with \code{NA} entity fields so every input is
represented.
}
\examples{
\dontrun{
hf_ner("Barack Obama was born in Hawaii.")

# One row per entity, ready to count or join
library(dplyr)
hf_ner(headlines) |>
  filter(!is.na(word)) |>
  count(entity_group, sort = TRUE)
}
}
\seealso{
\url{https://huggingface.co/docs/inference-providers/tasks/token-classification}
}
