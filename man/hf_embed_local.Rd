% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local.R
\name{hf_embed_local}
\alias{hf_embed_local}
\title{Generate Embeddings with a Loaded Local Model}
\usage{
hf_embed_local(
  text,
  model,
  batch_size = 32L,
  normalize = FALSE,
  progress = FALSE
)
}
\arguments{
\item{text}{Character vector of texts to embed.}

\item{model}{An \code{hf_local_model} handle loaded with \code{task = "embed"}.}

\item{batch_size}{Positive scalar integer. Python inference batch size.}

\item{normalize}{Logical. Normalize embeddings to unit length.}

\item{progress}{Logical. Display the Python encoding progress bar.}
}
\value{
A tibble with \code{text}, \code{embedding} (a list of numeric vectors),
and integer \code{n_dims}, matching the schema of \code{\link[=hf_embed]{hf_embed()}}.
}
\description{
Run SentenceTransformers on a reusable local handle. Prediction never
downloads a model or falls back to hosted inference. Input order and
duplicates are preserved. Missing texts retain \code{NULL} embeddings and
\code{NA_integer_} dimensions; zero-length and all-missing inputs do not call
Python. Empty strings are valid texts. The model's own maximum sequence
length controls truncation of long texts.
}
\examples{
\dontrun{
model <- hf_load_local_model(task = "embed")
hf_embed_local(c("Hello", NA, "Hello"), model, normalize = TRUE)
}
}
\seealso{
\code{\link[=hf_load_local_model]{hf_load_local_model()}}, \code{\link[=hf_classify_local]{hf_classify_local()}}
}
