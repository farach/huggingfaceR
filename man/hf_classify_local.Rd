% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local.R
\name{hf_classify_local}
\alias{hf_classify_local}
\title{Classify Text with a Loaded Local Model}
\usage{
hf_classify_local(text, model, batch_size = 32L, truncation = TRUE)
}
\arguments{
\item{text}{Character vector of texts to classify.}

\item{model}{An \code{hf_local_model} handle loaded with \code{task = "classify"}.}

\item{batch_size}{Positive scalar integer. Python inference batch size.}

\item{truncation}{Logical. Truncate long inputs to the tokenizer's maximum
length. With \code{FALSE}, overlong inputs may produce a backend error.}
}
\value{
A tibble with \code{text}, character \code{label}, and numeric \code{score},
matching the schema of \code{\link[=hf_classify]{hf_classify()}}.
}
\description{
Run a local text-classification pipeline, requesting the top label for each
text. Prediction never downloads a model or falls back to hosted inference.
Input order and duplicates are preserved. Missing and empty-string texts
retain missing labels and scores without being sent to Python. Zero-length
and entirely missing or empty inputs do not call the backend.
}
\details{
Scores are model outputs, not calibrated measures of certainty. By default,
long texts are truncated to the tokenizer's supported maximum length.
}
\examples{
\dontrun{
model <- hf_load_local_model(task = "classify")
hf_classify_local(c("I like this.", NA, "I dislike this."), model)
}
}
\seealso{
\code{\link[=hf_load_local_model]{hf_load_local_model()}}, \code{\link[=hf_embed_local]{hf_embed_local()}}
}
