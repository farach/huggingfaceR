% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text-tasks.R
\name{hf_table_question_answer}
\alias{hf_table_question_answer}
\title{Table Question Answering}
\usage{
hf_table_question_answer(
  query,
  table,
  model = hf_default_model("table_question_answer"),
  token = NULL,
  endpoint_url = NULL,
  ...
)
}
\arguments{
\item{query}{Character vector of question(s) to ask about the table.}

\item{table}{A data frame to query.}

\item{model}{Character string. Model ID from the Hugging Face Hub. Append
\code{":provider"} to select an inference provider. Default:
"google/tapas-base-finetuned-wtq".}

\item{token}{Character string or NULL. API token for authentication.}

\item{endpoint_url}{Character string or NULL. A custom Inference Endpoint URL.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A tibble with columns: query, answer, aggregator, cells (a
list-column of the source cells the answer was drawn from).
}
\description{
Ask a question in plain language about a data frame, using a table
question-answering model (such as TAPAS) via the Hugging Face Inference
Providers API. The data frame is converted to the string-cell format the API
expects; all values are coerced to character.
}
\examples{
\dontrun{
sales <- data.frame(
  product = c("Widgets", "Gadgets", "Gizmos"),
  revenue = c(120, 80, 50)
)
hf_table_question_answer("Which product had the highest revenue?", sales)
hf_table_question_answer("What is the total revenue?", sales)
}
}
\seealso{
\url{https://huggingface.co/docs/inference-providers/tasks/table-question-answering}
}
