% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embeddings.R
\name{hf_embed_umap}
\alias{hf_embed_umap}
\title{Dimensionality Reduction with UMAP}
\usage{
hf_embed_umap(
  text,
  model = "BAAI/bge-small-en-v1.5",
  token = NULL,
  n_neighbors = 15,
  min_dist = 0.1,
  ...
)
}
\arguments{
\item{text}{Character vector of text(s) to embed and reduce.}

\item{model}{Character string. Model ID for generating embeddings.
Default: "BAAI/bge-small-en-v1.5".}

\item{token}{Character string or NULL. API token for authentication.}

\item{n_neighbors}{Integer. UMAP n_neighbors parameter. Default: 15.}

\item{min_dist}{Numeric. UMAP min_dist parameter. Default: 0.1.}

\item{...}{Additional arguments passed to uwot::umap().}
}
\value{
A tibble with columns: text, umap_1, umap_2
}
\description{
Reduce embedding dimensions to 2D using UMAP for visualization.
Requires the 'uwot' package to be installed.
}
\examples{
\dontrun{
# Reduce and visualize
library(ggplot2)
texts <- c("cat", "dog", "kitten", "puppy", "car", "truck")
coords <- hf_embed_umap(texts)

ggplot(coords, aes(umap_1, umap_2, label = text)) +
  geom_text() +
  theme_minimal()
}
}
